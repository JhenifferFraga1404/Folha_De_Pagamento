@model RedefinirSenhaModel
@{
    Layout = "_LayoutDeslogado";
    ViewData["Title"] = "Redefinir Senha";

}

@if (TempData["MensagemErro"] != null)
{
    <div class="alert alert-danger" role="alert">
        <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="Close">X</button>
        @TempData["MensagemErro"]
    </div>
}

<form asp-controller="Login" asp-action="EnviarLinkParaRedefinirSenha" method="post">
    <div class="form-group">
        <div class="row">
            <div class="col-md-4">
            </div>
            <div class="col-md-4">
                <label for="login">Login</label>
                <input type="text" asp-for="Login" class="form-control" id="login">
                @Html.ValidationMessageFor(x => x.Login)
            </div>
            <div class="col-md-4">
            </div>
        </div>
    </div>
    <div class="form-group">
        <div class='row'>
            <div class="col-md-4">
            </div>
            <div class="col-md-4">
                <label for="email">E-mail</label>
                <input type="text" asp-for="Email" class="form-control" id="email">
                @Html.ValidationMessageFor(x => x.Email)
            </div>
            <div class="col-md-4">
            </div>
        </div>
    </div>
    <div class="col-md-4 mx-auto">
            <button type="submit" class="btn btn-primary w-100 md-4">Redefinir Senha</button>
            <a class="btn btn-secondary btn-block w-100 md-4" asp-action="Index" asp-controller="Login" role="button">Voltar</a>
    </div>
    <div class="col-md-4">
    </div>
    <
</form>